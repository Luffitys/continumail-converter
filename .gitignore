bin/
obj/
*.user
test_output.pst
.worktrees/
bench-output/
takeout-bench.json
testdata/

# Local test configs with machine-specific paths — never commit
fixtures/full-mail-config.json
fixtures/takeout-test-config.json

# Visual-companion brainstorm artifacts (local only)
.superpowers/

# Root session-handoff scratch note (local-only). Anchored to root so the tracked,
# export-ignored docs/SESSION-HANDOFF.md is NOT caught by this pattern.
/SESSION-HANDOFF.md

# Local manual-test output (PSTs / reports) — never commit
_manualtest/
